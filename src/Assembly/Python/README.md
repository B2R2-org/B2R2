# B2R2.Assembly.Python

Assembles Python bytecode from the syntax B2R2's Python disassembler
writes. One table per version, taken from that version's own opcode enum
in `B2R2.FrontEnd.Python`, so the assembler and the decoder agree by
construction rather than by review.
